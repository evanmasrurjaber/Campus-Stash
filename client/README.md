# CampusStash Frontend (React + Vite)

## Setup
1. Install dependencies:
	- `npm install`
2. Start frontend dev server:
	- `npm run dev`

## Environment Variables

- `VITE_API_BASE_URL` example: `http://localhost:5000/api`

